export enum Database {
    T5Todos = "T5-ToDos"
}

export enum Collections {
    Ger = "Ger",
    Users = "Users",
    Lists = "Lists"
}

export enum Errors {
    callToAdmin = "call to admin",
    noData = "data was not provide",
    dbError = "db error"
}